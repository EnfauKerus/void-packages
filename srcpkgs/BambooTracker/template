# Template file for 'BambooTracker'
pkgname=BambooTracker
version=0.4.5
revision=1
build_wrksrc="BambooTracker"
build_helper=qmake
build_style=qmake
configure_args="CONFIG-=debug CONFIG+=release $(vopt_if pulseaudio CONFIG+=use_pulse) $(vopt_if jack CONFIG+=use_jack) $(vopt_if jack CONFIG+=jack_has_rename)"
makedepends="qt5-multimedia-devel alsa-lib-devel $(vopt_if pulseaudio pulseaudio-devel) $(vopt_if jack jack-devel)"
short_desc="YM2608 (OPNA) music tracker"
maintainer="Enfau Kerus <kerus@enfa.uk>"
license="GPL-2.0-only"
homepage="https://github.com/rerrahkr/BambooTracker"
distfiles="https://github.com/rerrahkr/BambooTracker/archive/v${version}.tar.gz"
checksum=b31221353f54e81edbeb301c125efc84c69deb2c943be305601310bf5cc3cc0e

build_options="jack pulseaudio"
build_options_default="jack pulseaudio"

if [ "$CROSS_BUILD" ]; then
	hostmakedepends+=" qt5-multimedia-devel"
fi
